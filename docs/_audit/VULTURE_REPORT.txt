Vulture report — 2026-05-07 (post Faz 13)
==========================================

Command:
    python3 -m vulture core/ modules/ utils/ scripts/ scanner.py api_server.py

Config (pyproject.toml [tool.vulture]):
    min_confidence = 80
    exclude        = tests/, core/ai_skills/, core/governance/, tools/, venv/, .venv/
    ignore_names   = signum, frame, http
        (signum/frame: signal handler protocol;
         http: used in string type annotation in modules/proxy_interceptor.py
         that vulture's AST pass does not see.)

Result: 0 findings.

Triage history (before fix)
---------------------------

Pre-fix vulture run reported 5 findings:

    modules/cloud_enum.py:477       unused variable 'platform_key'
    modules/jwt_attack.py:327       unused variable 'original_response'
    modules/proxy_interceptor.py:24 unused import 'http'           (false positive)
    utils/agent_framework/orchestrator.py:63  signum, frame        (false positives)

Resolutions:
    - cloud_enum:           platform_key is now propagated into the
                            cicd_exposure finding dict as a stable
                            machine-readable key alongside the
                            human-readable platform_info["name"].
    - jwt_attack:           original_response was passed `None` by all
                            three callers; removed from signature and
                            callers.
    - proxy_interceptor:    `http` is referenced in the string type
                            annotation "http.HTTPFlow"; ignored via
                            `ignore_names`.
    - orchestrator handler: `signum`/`frame` are required by Python's
                            signal handler ABI; ignored via
                            `ignore_names`.

How to re-run
-------------

    python3 -m vulture core/ modules/ utils/ scripts/ scanner.py api_server.py

Vulture reads its config from pyproject.toml [tool.vulture] so no
extra flags are needed.
