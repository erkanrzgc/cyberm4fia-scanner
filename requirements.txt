# scanner dependencies (consolidated)
#
# Run: pip install -r requirements.txt
# Everything ships in one file by design — no optional/extras split.

# ── Core HTTP / parsing / async ────────────────────────────────────────
httpx[http2]>=0.27.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# ── Headless browser (Playwright active verifiers + dynamic crawler) ──
playwright>=1.40.0

# ── CLI / output ───────────────────────────────────────────────────────
rich>=13.0

# ── Crypto / JWT / OOB (Interactsh) ────────────────────────────────────
cryptography>=41.0
pyjwt>=2.8.0

# ── YAML (template engine, GTFOBins) ───────────────────────────────────
pyyaml>=6.0

# ── DNS recon ──────────────────────────────────────────────────────────
dnspython>=2.4.0

# ── Environment / Secret Management ────────────────────────────────────
python-dotenv>=1.0.0

# ── REST API server (--api flag) ───────────────────────────────────────
fastapi[standard]>=0.110.0
urllib3>=1.26.0

# ── MITM proxy interceptor (--proxy-listen) ────────────────────────────
# bcrypt is pinned <4.1 because newer bcrypt + current passlib raises
# "ValueError: password cannot be longer than 72 bytes" at mitmproxy
# import time. Remove the pin once passlib ships the truncation patch.
mitmproxy>=10.0
passlib>=1.7.4
bcrypt>=3.2.0,<4.1

# ── QR-phishing detection (Qishing module) ─────────────────────────────
Pillow>=10.0
pyzbar>=0.1.9

# ── SSH/FTP brute-force (--spray with SSH/FTP targets) ─────────────────
paramiko>=3.4.0

# ── LLM vulnerability scanner (--llm-probe) ────────────────────────────
# garak ships its own deps (openai SDK, jsonlines, etc.) — let pip
# resolve. Provides probe catalog for prompt injection / jailbreak /
# data leakage on detected LLM endpoints.
garak>=0.10
